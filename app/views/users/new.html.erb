<%= provide(:title, "Sign up") %>

<div class="form jumbotron bg-secondary text-light">
    <div class="row">
        <div class="col-6 mx-auto">
            <h1 class="text-center display-4">Sign up</h1>
            <p class="text-center mt-2 mb-4">Join us for free!</p>
            <%= render "form", btn_msg: "Create a new account" %>
            <p>Already have an account? <%= link_to "Login!", login_path %></p>
        </div>
    </div>
</div>